// This is a client-side loader that doesn't load any data
// The Essays component will load data directly from Supabase

export function load() {
  return {
    // Return empty data - component will handle loading
  };
} 